// Package imports
// > Components
import {Box, LinearProgress} from '@mui/material';

export default function LoadingLine() {
  return (
    <Box sx={{width: '100%'}}>
      <LinearProgress />
    </Box>
  );
}
