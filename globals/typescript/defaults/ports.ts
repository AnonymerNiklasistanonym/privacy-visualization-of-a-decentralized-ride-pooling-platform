/** The default ports. */
export const ports = Object.freeze({
  /** The default port of the simulation. */
  simulation: 2222,
  /** The default port of the visualization. */
  visualization: 3000,
});
