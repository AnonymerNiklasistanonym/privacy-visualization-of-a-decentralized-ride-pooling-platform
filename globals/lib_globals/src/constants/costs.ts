/* The default costs of actions. */

/** The average cost per kilometer in a ride for the customer (Euro). */
export const avgCostRideKilometer = 0.35;
/** The base cost per ride for the customer (Euro). */
export const baseCostRide = 2;
